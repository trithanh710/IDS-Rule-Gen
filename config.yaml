network:
  home_net: "$HOME_NET"
  external_net: "$EXTERNAL_NET"

rule:
  action: "alert"
  msg_prefix: "Blacklisted IP"
  start_sid: 1000001
  rev: 1

output:
  default_file: "data/output.rules"

logging:
  level: "INFO"
